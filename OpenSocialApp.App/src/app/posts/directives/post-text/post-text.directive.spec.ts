import { PostTextDirective } from './post-text.directive';

describe('PostTextDirective', () => {
  it('should create an instance', () => {
    const directive = new PostTextDirective();
    expect(directive).toBeTruthy();
  });
});
