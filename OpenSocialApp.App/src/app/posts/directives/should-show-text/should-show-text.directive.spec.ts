import { ShouldShowTextDirective } from './should-show-text.directive';

describe('ShouldShowTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShouldShowTextDirective();
    expect(directive).toBeTruthy();
  });
});
